import Image from "next/image";
import Link from "next/link";


export default async function RootLayout({
    children,
  }: Readonly<{
    children: React.ReactNode;
  }>) {

    return (
    <main className="flex min-h-screen w-full justify-between font-inter overflow-y-hidden no-scrollbar bg-sky-1">

      {children}
      <div className="auth-asset">
        <Link href={"/"}>
          <Image 
            src="/logo/spacematch-logo.png"
            alt="Auth image"
            width={300}
            height={300}
            className="rounded-full object-contain shadow-md"
          />
        </Link>
      </div>
    </main>
    );
}
  